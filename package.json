{
  "name": "homepage",
  "type": "module",
  "private": true,
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0",
    "npm": ">=10.9.8"
  },
  "scripts": {
    "check": "astro check && npm run check:tokens",
    "check:tokens": "node scripts/check-design-tokens.mjs",
    "build": "npm run check:tokens && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "7.2.4"
  },
  "devDependencies": {
    "typescript": "^6.0.0",
    "@astrojs/check": "^0.9.10"
  }
}
